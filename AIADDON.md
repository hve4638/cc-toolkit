# aiaddon

에이전트 부가 기능을 어디서 켜고 끄는지 한 곳에 적어두는 파일 형식. 지식·지시·기능·규칙을 항목 이름으로 나열하고, 읽는 쪽이 아는 이름만 골라 쓰고 모르는 이름은 무시한다.

특정 하네스에 매이지 않는다. 이 문서는 파일을 어떻게 파싱하고 어떤 항목이 켜진 상태인지까지만 정한다. 항목 이름이 무엇을 뜻하는지, 켜졌을 때 무슨 일이 일어나는지는 그 이름을 아는 구현이 정한다.

작성일 2026-08-10. 첫 구현 예정지는 cc-toolkit 의 `core`.

## 위치

| 층 | 경로 |
|---|---|
| 전역 | `~/.config/aiaddon/` |
| 로컬 | `<세션 루트>/.config/aiaddon/` |

세션 루트는 세션이 시작된 위치다 (Claude Code 기준으로 `.claude` 가 있는 곳, `.agent-memory` 가 생성되는 것과 같은 기준). 세션 시작 시점에 고정되며 이후 작업 디렉터리가 바뀌어도 따라가지 않는다. 상위 디렉터리로 거슬러 탐색하지 않는다.

## 네임스페이스

폴더 안의 파일 하나가 네임스페이스 하나다. 이름은 고정이다.

| 파일 | 읽는 쪽 |
|---|---|
| `event` | 세션 시작·도구 호출처럼 성긴 시점에 도는 훅 |
| `statusline` | 상태줄 — 세션마다 초 단위로 갱신되며 그때마다 읽는다 |

나누는 축은 항목의 내용이 아니라 읽는 쪽의 주기다. 매번 읽는 소비자에게 성긴 쪽 내용까지 딸려가지 않게 갈랐다. 그래서 목록은 짧고, 주기가 다른 소비자가 생길 때만 는다. 목록에 없는 이름의 파일은 무시한다.

전역과 로컬은 **같은 이름의 파일끼리** 짝지어 해석한다. 한쪽에만 있으면 있는 쪽만 쓴다.

네임스페이스는 서로 독립이다. 한 파일의 줄은 다른 파일의 항목에 영향을 주지 않으며, 같은 이름이 두 파일에 있어도 별개 항목이다. `!*` 도 그 파일 안에서만 유효하다.

## 해석

전역 파일과 로컬 파일을 그 순서로 이어붙인 뒤 위에서 아래로 읽는다. 한 항목에 **마지막으로 매치된 줄**이 그 항목의 상태를 정한다.

- 한 줄에 항목 하나. 줄 앞뒤 공백은 무시한다.
- 빈 줄과 `#` 로 시작하는 줄은 무시한다.
- 리더가 모르는 항목은 무시한다.

## 항목

```
<종류>:<이름>[@<인자>]
```

종류와 이름은 소문자 영문자·숫자·`-` 로 쓴다. 종류 목록은 고정하지 않고 자유롭게 는다 — 지금까지 쓰인 것은 `feat:`, `inst:`, `rule:`, `know:`.

`!` 를 앞에 붙이면 그 줄이 매치하는 항목을 끈다.

`*` 는 부정에서만 쓴다. `:` 를 포함해 아무 문자에나 매치하므로 `!know:*` 는 `know:` 로 시작하는 전부를, `!*` 는 그 파일의 전부를 끈다. `!*` 뒤에 다시 적은 항목은 켜진다. 켜는 쪽에는 `*` 를 두지 않는다 — 켜는 것은 언제나 이름을 적는다.

```
# 전부 끄고 필요한 것만
!*
know:rust-embedded
rule:conventional-commit
```

## 인자

`@` 뒤에 쉼표로 구분해 적는다. 인자는 전부 이름을 갖는다.

- `키=값` 형태. 값 없이 키만 오면 그 키가 참이라는 뜻이다.
- 키는 영문자 또는 `_` 로 시작하고 이후 영문자·숫자·`_`. 그 밖의 문자는 쓰지 않는다.
- 값에는 공백과 `,` 와 `=` 를 쓸 수 없다. 인용부호는 없다.
- 위치 인자는 없다.

```
feat:advertise@lang=ko,simplify
```

부정은 **이름만 본다**. `!feat:*` 는 인자와 무관하게 끈다.

덮어쓰기는 **줄 통째로**다. 뒤에 온 줄의 인자 집합이 앞의 것을 대체하며, 키 단위로 병합하지 않는다.

이 문서는 인자의 문법만 정한다. 키와 값이 무엇을 뜻하는지는 그 항목을 아는 구현이 정한다.

## 스위치와 설정의 분리

aiaddon 은 **무엇을 켜는지**만 갖는다. 기능의 설정 본문은 그 기능이 자기 파일에 갖는다.

예를 들어 `rule:banaction` 이 켜지면 리더가 프로젝트 루트의 `.banaction` 을 읽는다. `.banaction` 의 규칙들이 aiaddon 으로 옮겨오지는 않는다. 인자는 켜는 김에 붙이는 작은 손잡이지 설정을 옮겨 담는 그릇이 아니다.

## 미정

- 어느 리더도 읽지 않은 항목 (오타 등) 을 사용자에게 알리는 방법
- 쓸 수 있는 항목 목록을 사용자가 확인하는 방법
- `@include` — 다른 파일의 내용을 그 자리에 그대로 펼치는 지시. C 의 헤더처럼 순수 삽입이라 해석 순서에 예외를 만들지 않는다 (뒤에 오는 `!*` 는 펼쳐진 것까지 지운다). 지금은 넣지 않는다.
