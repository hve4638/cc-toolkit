# CLAUDE.md

## 작업 맥락

이 저장소는 Claude Code 플러그인(core, frame 등)을 만들고 보수하는 작업 공간이다. 플러그인 스펙이 필요하면 [Claude Plugin Docs](https://code.claude.com/docs/llms.txt)를 참조한다.

사용자가 "skill" 또는 "hook"을 보라고 하면, Claude 자신에게 활성화된 것이 아니라 지금 작업 중인 플러그인의 `skills/`·`hooks/` 폴더 안 파일을 가리킨다.

## 메타 원칙 — 가드레일 vs 지침

스킬·문서·규칙에 새 규칙을 넣기 전에 먼저 분류한다:

- `가드레일` (구조적 제약 — 출력 형식, 모듈 경계, 검증 가능한 산출물 등) 이면 강제·구체적으로 명시한다. 모델 성능과 무관하게 유지되기 때문이다.
- `지침` (행동 제약 — 에이전트가 추론·판정·해석하는 방식) 이면 최소한의 권고에 그친다. 모델 성능에 따라 필요 없어지거나 다르게 해석될 여지가 있기 때문이다.

행동 처방을 가드레일 (구조·투명성 강제) 로 우회 표현 가능하면 그 쪽이 낫다. 예: "X 를 하지 마라" (지침) → "X 를 했다면 명시한다" (가드레일). 전체 원칙은 `core/skills/writing-great-agents-md/SKILL.md` 를 기준으로 한다.

## SKILL 작성법

### `$ARGUMENTS` 는 변수가 아니라 substitution point

`SKILL.md` 본문의 `$ARGUMENTS` 는 호출 시점에 실제 사용자 입력으로 치환된 후 에이전트에게 전달된다.

따라서 본문에 `$ARGUMENTS 가 X 면` 같은 조건문을 쓰면, 치환 후 `<실제 입력값> 가 X 면` 형태가 되어 가짜 변수 참조처럼 어색해진다.

본문에서 사용자 입력을 지칭할 때는 자연어 표현을 쓴다:
- ❌ `$ARGUMENTS 가 slug 형태면 그대로 사용한다`
- ✅ `사용자가 제공한 인자가 slug 형태면 그대로 사용한다`

예외: 본문 끝의 `Task: $ARGUMENTS` 컨벤션은 substitution 자체가 목적이므로 그대로 둔다.

### SKILL.md / SKILL.ko.md 페어 동기화

같은 폴더에 `SKILL.md` 와 `SKILL.ko.md` 가 둘 다 있으면 영·한 번역 페어로 취급한다. 한쪽을 수정하면 같은 세션에서 다른 쪽도 같이 갱신한다.

페어 판정 기준은 섹션 구조, bullet 개수, 코드펜스 안 식별자·메시지, frontmatter 의 기능 필드 일치다. 자연어 번역 차이는 허용한다.

### 본문은 실행 모델이 쓰는 것만 담는다

`SKILL.md` 본문은 치환 후 그 스킬을 실행하는 모델에게 전달된다. 본문 절은 그 모델이 런타임에 실제로 쓰는 것만 담는다. 형제 스킬과의 경계 대조나 카탈로그성 설명은 대개 저자·유지보수자용 문서다. 절을 지웠을 때 실행 모델의 행동이 달라지지 않으면 뺀다.

## 작업 디시플린 (재발 방지)

세션에서 드러난 패턴에 대한 재발 대책.

### 1. 메커니즘 단언 전 검증

도구·시스템 동작에 대한 단언은 직접 인용 가능한 출처 (시스템 프롬프트·Skill 도구 description·docs·코드) 로 뒷받침한다. 출처를 못 대면 "확인 필요" 라고 명시한 채 답하거나, grep·Read 로 확인 후 답한다.

적색 신호: "...로 보입니다·...일 것 같습니다·그러므로 ... 가능합니다" 식 추론을 추론으로 잇는 표현이 누적될 때 — 검증을 건너뛰고 있다는 뜻.

### 2. `replace_all` 의 의미 분기 명시

같은 토큰을 일괄 치환하기 전, 각 출현 위치가 동일 개념을 가리키는지 한 번 더 확인한다. 한 곳이라도 의미가 갈라지면 `replace_all` 대신 행마다 Edit. 일괄 처리한다면 "모두 동일 개념이라 판단" 이라고 짧게 명시.

특히 치환 대상이 코드 식별자가 아니라 *문장 속 명사* 일 때 거의 항상 의미가 갈라진다.

### 3. 부드러운 권고에는 근거를 묶는다

"자연스럽다·맞아 보인다·좋을 것 같다" 류 권고에 1줄 근거를 붙인다. 근거가 약하면 "근거 약함, 결정 필요" 라고 명시하고 사용자 판단에 맡긴다.

근거 없는 부드러운 권고는 *판단 부담을 사용자에게 슬쩍 넘기는* 형태로 끝맺어 사후 반박을 유발한다.

### 4. Step-0 — 변경의 최종 목표 확인

이름 변경·삭제·플래그 토글 등 *기본 사용 패턴을 깨는* 변경 요청은 진행 전 한 번 묻는다: "이게 다음 단계로 이어지는 변화인가, 단발인가?"

특히 변경 후 파일·스킬이 *부분적으로 깨진 상태* (예: frontmatter 의 핵심 필드가 빠진 채) 가 되면 강한 신호 — 사용자에게 후속 계획이 있다는 뜻일 가능성 높음.

### 5. 기본 동작을 지시하지 않는다

규칙·문서에 *없으면 어차피 그렇게 되는* 동작을 지시로 적지 않는다. 정보를 안 주면 에이전트는 당연히 그것을 모르므로, "미리 알 필요 없다·필요할 때 찾아라" 류 문장은 행동을 바꾸지 않는 빈 지시다.

판정: 그 줄을 지웠을 때 에이전트 행동이 달라지는가? 안 달라지면 사족이니 뺀다.

## 플러그인 작업 규칙

### 프로젝트 스코프 `.agent-memory` 쓰기는 lib 경유

`<projectRoot>/.agent-memory/` 아래에 상태 파일을 만드는 core 코드는 `core/scripts/lib/agent-memory.mjs` 를 경유한다 — 직접 mkdir 금지. "워크스페이스가 없으면 아무것도 만들지 않는다" 가드 (wt-destroy 된 워크트리 부활 방지) 를 이 lib 한 곳이 보장한다. lib 을 import 할 수 없는 곳 (타 플러그인, TS 번들) 은 같은 가드를 자체 구현하고 WHY 주석으로 명시한다.

### /stage 호출 시 버전 bump 확인

`/stage` (`core:stage`) 호출 시, 변경된 파일이 특정 플러그인 디렉터리 (예: `core/`, `oc-browser/`) 안에 있으면 해당 플러그인의 `.claude-plugin/plugin.json` 의 `version` 이 *이번 변경분에 맞춰 bump 됐는지* 먼저 확인한다. 안 했으면 적절한 단계로 bump 한 뒤 같이 스테이징한다.

bump 단계 기준:
- patch — 오탈자·사소한 문구 수정·주석 등 minor 미만 변경
- minor — 그 외 (기능 추가·확장, 리팩토링, 새 스킬·에이전트 추가, 규칙 변경)
- major — 사용자 명시 시에만