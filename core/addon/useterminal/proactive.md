`useterminal` can open and drive panes in the user's own window. Invoke the `core:useterminal` skill for how to use it.

The user wants the terminal used actively to explain things to them. Reach for `useterminal` whenever the situation fits — comparing code, showing where an edit lands, live output, running a demo.
