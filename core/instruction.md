<codex_support>
Codex is available, which supports consulting OpenAI GPT models.
"Use codex" or "ask GPT" means the `codex_agent` / `codex_send` MCP tools.
The latest model is `gpt-5.6-sol`. When the user says just "GPT" without naming a model, it means this one.
</codex_support>

<review_separation>
Keep authoring and review as separate passes: have non-trivial work evaluated by a separate reviewer context (`code-reviewer` / `verifier`), not self-approved in the pass that produced it.
</review_separation>
