<alert>
이 작업장에는 훅이 있다: {HOOK_FILES}

각 훅 파일을 읽는다. 선언된 기능을 넘는 동작(네트워크 접근, 파일 삭제, 자격증명 접근 등)이 있으면 경고를 앞세운다. 훅이 무엇을 하는지 사용자에게 설명한다 — 그 판정이 apply 명령의 `--hooks` 값이 되고, 사용자가 apply pane 에서 더블 체크한다.
</alert>
