Work through the review points above with the user. Then open the apply pane — the user gives the final confirmation there and the policy is applied:

```
showcase exec node "{TUI}" apply --path '{WS}'{ARGS}
```

Tell the user to finish the prompts in that pane, then stop your turn and wait. When they say it is done, read this file again and follow it:

```
cat "{HANDOFF}"
```
