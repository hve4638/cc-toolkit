{N}. where (select one)
- "../": repo 옆
- "./": repo 안

어느 쪽이든 폴더명은 {WORKTREES_NAME} 다. 사용자가 다른 이름·위치를 원하면 여기서는 가까운 쪽을 고르고, step 2 를 돌리기 전에 작업장 `settings` 의 `worktree-dir = <원하는 경로>` (repo 루트 기준 상대경로) 를 직접 고친다.
