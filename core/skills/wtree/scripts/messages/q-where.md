{N}. where (select one)
- "../": beside the repo
- "./": inside the repo

Either way the folder is named {WORKTREES_NAME}. If the user wants a different name or location, pick the closer option here, then set `worktree-dir = <their path>` (relative to the repo root) in the workspace `settings` before running step 2.
