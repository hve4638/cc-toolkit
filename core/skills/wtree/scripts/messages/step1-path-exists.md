<status>Error</status>
<error>
{PATH} already exists and is not a policy workspace. `path` names a fresh folder where the policy is composed — it is not where worktrees will live. Nothing was done.
</error>
<next>
Re-run step 1 with no arguments — it reads the current state and guides the route:

```
node {STEP1}
```
</next>
