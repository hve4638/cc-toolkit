<status>Error</status>
<error>
{PATH} already exists. Nothing was done.
</error>
<next>
Re-run step 1 with no arguments — it reads the current state and guides the route:

```
node {STEP1}
```
</next>
