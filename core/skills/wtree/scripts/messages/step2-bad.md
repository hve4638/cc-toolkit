<status>Error</status>
<error>
{PROBLEM}. Nothing was applied.
</error>

<info>
If the workspace is incomplete (e.g. a custom shape whose rules is not written yet), complete it and re-run the same command. If path differs from what the previous step printed, call it as that output says.
</info>
