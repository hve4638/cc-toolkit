<status>Error</status>
<error>
`wtree init --load {DIR}` 가 거부했다. repo 정책에는 아무것도 반영되지 않았다.

```
{WTREE_OUTPUT}
```
</error>

<info>
출력이 가리키는 문제를 작업장에서 고친 뒤 같은 명령을 재실행한다. 원인이 사용자 결정에 걸려 있으면 출력을 그대로 전달하고 묻는다.
</info>
