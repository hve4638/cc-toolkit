<status>Error</status>
<error>
`wtree init --load {DIR}` rejected it. Nothing was applied to the repo policy.

```
{WTREE_OUTPUT}
```
</error>

<info>
Fix what the output points at in the workspace and re-run the same command. When the cause hangs on a user decision, relay the output as-is and ask.
</info>
