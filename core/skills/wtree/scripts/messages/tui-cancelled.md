<status>Error</status>
<error>
The user backed out of the TUI pane. This run changed nothing new — a workspace already created by an earlier run, if any, is untouched.
</error>
<next>
Ask the user how to continue: relaunch the same pane, or switch to the step scripts — starting read-only:

```
node {STEP1}
```
</next>
