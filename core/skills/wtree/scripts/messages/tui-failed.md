<status>Error</status>
<error>
The TUI hit an internal error and stopped:

```
{DETAIL}
```
</error>
<next>
Check the current state with the read-only step 1 query, then continue on the step route:

```
node {STEP1}
```
</next>
