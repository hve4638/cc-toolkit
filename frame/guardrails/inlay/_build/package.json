{
  "name": "inlay-plugin-build",
  "version": "0.1.0",
  "description": "Build workspace for inlay MCP server (INLAY.md search)",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
