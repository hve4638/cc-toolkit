{
  "description": "personal hook — SessionStart personal instruction inject",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|compact|clear",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}\"/scripts/run.cjs \"${CLAUDE_PLUGIN_ROOT}\"/scripts/session-start-inject.mjs",
            "timeout": 3
          }
        ]
      }
    ]
  }
}
